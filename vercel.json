{
  "buildCommand": "cd book-library/frontend && npm install && npm run build",
  "outputDirectory": "book-library/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
